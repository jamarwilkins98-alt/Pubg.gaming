const form=document.getElementById("registrationForm"),message=document.getElementById("message");form.addEventListener("submit",async e=>{e.preventDefault();message.textContent="Submitting...";const data=Object.fromEntries(new FormData(form));try{const res=await fetch("/api/registrations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const body=await res.json();if(!res.ok)throw new Error(body.error||"Registration failed");message.textContent="Registration submitted successfully. Status: Pending.";form.reset()}catch(err){message.textContent=err.message+" (API is not deployed yet.)"}});